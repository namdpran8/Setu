# SetuLauncher

This project is a standalone companion to the main Setu runtime. It provides a launcher UI for discovering and selecting apps. Once implemented, it will spawn `setu_runtime.exe` as a child process to run the selected Android package.
